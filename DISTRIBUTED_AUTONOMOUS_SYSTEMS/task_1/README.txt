_ all files which start with "1" are relative to the distributed optimization of a quadratic cost function without gradient tracking (Folder "1")

_ all files which start with "2" are relative to the distributed optimization of a quadratic cost function with gradient tracking (Folder "2")

_ all files which start with "3" are relative to the distributed learning of a NN without gradient tracking (Folder "3")

_ all files which start with "4" are relative to the distributed learning of a NN with gradient tracking (Folder "4")